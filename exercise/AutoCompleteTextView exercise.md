```xml
<AutoCompleteTextView
    android:id="@+id/autoCompleteTextView1" 
    android:layout_width="match_parent" 
    android:layout_height="wrap_content" 
    android:completionThreshold="1"
    android:ems="10" />
```

Now create a file `exercise_autocomplete` file in layout subfolder and try the above example with different attributes of `AutoCompleteTextView` in Layout XML file as well at programming time to have a different look and feel. 

- Try to make it editable
- Change the font color, font family, width, textSize etc and see the result.
- Try the above example with multiple `AutoCompleteTextView` controls in one activity.

---
> [!NOTE] 
> 🔗 This exercise uses legacy Views. For the modern Material Design 3 approach, please see [[AutoCompleteTextView modern exercise]].

**Index:** [[Android UI Components Index]]
